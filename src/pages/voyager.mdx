---
layout: ../layouts/ExhibitLayout.astro
title: "The 2024 Voyager 1 FDS Memory Rescue"
description: "Reallocating Voyager 1's Fragmented Memory Map"
author: "Gonzaga, Rainer D.; Gonzales, Aaron James S.; Manalang, Kennese Ross F.; Marcaida, Duncan Joseph B.; Ramos, Richmond Jose G."
readingTime: "10 minute read"
---

import VoyagerScrollyTelling from '../components/VoyagerScrollyTelling.tsx';

import '@fontsource/archivo-black';
import '@fontsource-variable/space-grotesk';
import '@fontsource-variable/roboto-mono';
import '@fontsource/share-tech-mono';

<style>
{`
  /* This template ships Tailwind with preflight disabled, so nothing sets
     box-sizing and every element defaults to content-box — which makes any
     w-full element with padding overflow its parent (boxes ran off the screen
     edge on mobile). Restore the border-box everyone expects, scoped to this
     page. */
  *, *::before, *::after {
    box-sizing: border-box;
  }
  html {
    background: #050505 !important;
  }
  body {
    background: transparent !important;
    color: #F8F8FF !important;
  }
  .article h1:first-of-type,
  .article :global(h1:first-of-type) {
    display: block !important;
  }
  .header {
    display: none !important;
  }
  .content__titlemeta {
    display: none !important;
  }
  .article {
    background: transparent !important;
    border: none !important;
    box-shadow: none !important;
    padding: 0 !important;
  }
  .article h2, .article h3, .article h4, .article h5, .article h6 {
    font-family: inherit !important;
    border-bottom: none !important;
  }
  .content {
    background: transparent !important;
    padding: 0 !important;
    max-width: 100% !important;
    display: block !important;
  }
  .content__block {
    display: block !important;
    flex: unset !important;
    gap: 0 !important;
  }
  footer {
    background-color: #090909 !important;
    color: #D3D3D3 !important;
    border-top: 1px solid rgba(250, 102, 2, 0.35) !important;
    margin-top: 6rem !important;
  }
`}
</style>

<VoyagerScrollyTelling client:load />
